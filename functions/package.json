{
  "name": "minegocio-pos-functions",
  "description": "Cloud Functions para notificaciones push",
  "scripts": { "deploy": "firebase deploy --only functions" },
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin":     "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
